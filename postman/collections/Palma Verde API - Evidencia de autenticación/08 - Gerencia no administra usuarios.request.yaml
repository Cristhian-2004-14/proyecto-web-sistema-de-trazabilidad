$kind: http-request
url: "{{baseUrl}}/api/users"
method: GET
headers:
  Authorization: Bearer {{limitedToken}}
scripts:
  - type: afterResponse
    code: pm.test('Rol sin permiso devuelve 403', () => pm.response.to.have.status(403));
order: 8000
