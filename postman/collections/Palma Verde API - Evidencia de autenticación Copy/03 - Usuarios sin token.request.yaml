$kind: http-request
url: "{{baseUrl}}/api/users"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Endpoint protegido devuelve 401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 3000
